# Docker Compose 插值变量：cp .env.example .env 后修改
# 这两个密码要与 backend/.env 中 DATABASE_URL 里的密码保持一致

MYSQL_ROOT_PASSWORD=<CHANGE_ME_root密码>
MYSQL_PASSWORD=<CHANGE_ME_quizapp用户密码>
# Use URL-safe hex, for example: openssl rand -hex 32
REDIS_PASSWORD=<CHANGE_ME_64位十六进制Redis密码>
